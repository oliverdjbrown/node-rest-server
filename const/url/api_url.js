const api = require('./base_url');

const path = {
    auth: `${api}/auth`,
    categories: `${api}/categories`,
    products: `${api}/products`,
    search: `${api}/search`,
    users: `${api}/users`,
};


module.exports = path